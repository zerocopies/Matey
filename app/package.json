{
  "name": "ai-concierge-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:mobile": "rm -rf dist && cp -r public dist && npx cap sync && cd android && ./gradlew assembleDebug && adb -s adb-43161JEKB05998-gdQbKt._adb-tls-connect._tcp install -r app/build/outputs/apk/debug/app-debug.apk",
    "cap:sync": "npm run build && cap sync",
    "cap:sync:mobile": "npm run build:mobile && cap sync android",
    "cap:open": "npm run build && cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/preferences": "^7.0.4",
    "lucide-react": "^1.31.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0",
    "@tailwindcss/vite": "^4.3.3",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
